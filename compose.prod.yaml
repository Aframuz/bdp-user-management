# Stack de producción. Este archivo vive en el servidor (lo copia el workflow
# de despliegue en cada corrida) y consume el `.env` que está junto a él.
#
#   docker compose -f compose.prod.yaml pull
#   docker compose -f compose.prod.yaml up -d
#
# Las imágenes no se construyen aquí: llegan ya publicadas desde GHCR.
#
# El stack NO atiende 80/443: Apache los ocupa en el host para servir otros
# sitios y hace de proxy inverso hacia el puerto local de aquí abajo.

name: bdp-user-management

services:
  web:
    image: ${IMAGE_REPOSITORY:?falta IMAGE_REPOSITORY}/web:${IMAGE_TAG:?falta IMAGE_TAG}
    restart: unless-stopped
    ports:
      # Atado a 127.0.0.1 a propósito: quien termina el TLS y atiende 80/443 es
      # Apache en el host, que reenvía aquí. Publicarlo en 0.0.0.0 expondría el
      # sitio en claro y saltándose el proxy.
      - "127.0.0.1:${HTTP_BIND_PORT:-8080}:8080"
    depends_on:
      app:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:8080/up"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  app:
    image: ${IMAGE_REPOSITORY:?falta IMAGE_REPOSITORY}/app:${IMAGE_TAG:?falta IMAGE_TAG}
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - app_storage:/var/www/html/storage
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      # php-fpm no expone HTTP; basta con comprobar que acepta conexiones.
      test: ["CMD", "php", "-r", "exit(@fsockopen('127.0.0.1', 9000) ? 0 : 1);"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE:?falta DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME:?falta DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?falta DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    # Sin `ports`: la base solo se alcanza desde la red interna de compose.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
      interval: 10s
      timeout: 3s
      retries: 10

volumes:
  app_storage:
  postgres_data:
